# AnythingLLM OpenAI-compatible base (Desktop/Docker default)
ANYTHINGLLM_OPENAI_BASE=http://localhost:3001/api/v1/openai


# API key from AnythingLLM (Settings → API Keys)
ANYTHINGLLM_API_KEY=replace-me


# Workspace slug to chat against (AnythingLLM treats this as the "model")
ANYTHINGLLM_WORKSPACE_SLUG=local


# Optional: Custom system prompt for conversations
# SYSTEM_PROMPT=You are a helpful, concise assistant.


# Optional: Text-to-speech voice for walkie.sh (run 'say -v ?' to see all voices)
# TTS_VOICE=Lee (Premium)